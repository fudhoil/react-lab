import React, { Component } from "react";

const NavBar = () => {
  return (
    <div className="navbar bg-primary">
      <span className="badge bg-light text-primary m-2 mt-1 mb-1">Navbar</span>
    </div>
  );
};

export default NavBar;
